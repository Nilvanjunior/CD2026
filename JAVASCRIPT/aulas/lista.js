let frutas = ['maça', 'uva', 'jaca', 'tomate']
// frutas.push("banana") // adiciona novos elementos ao fim da lista, por ex: banana
// frutas.unshift("goiaba") // adicione um novo elemento ao inicio da lista, por ex: goiaba
//frutas.pop() // remove o ultimo elemento
//frutas.shift() // remove o primeiro elemento
//frutas.splice(0, 1) // remove um elemento a partir do index selecionado, nesse caso: maça
// console.log(frutas.indexOf('maça')) // encontra o index a partir do elemento, nesse caso: 0
// console.log(frutas.length) // saber quantos elementos tem na lista

for (let i = 0; i < frutas.length; i++) {    //}>
    console.log(frutas[i])                   //} > exibir todos os elementos da lista sem saber quantos elementos ela tem
}                                            //}>